# 隐私与数据使用

本说明适用于 ChatHelp Windows 1.0.0-glm。程序不登录开发者服务，没有自建中转服务器或应用分析服务。原版自动更新检查已禁用，请从本分支 Release 获取更新。

开启采集后使用 Windows Graphics Capture 读取微信窗口截图，在本机用随包提供的 RapidOCR/ONNX 模型识别文字。不把截图上传给智谱，正常流程不保存截图文件。调试视图仅显示本机画面。退出程序后内存中的截图和会话历史释放。

分析时将聊天文字、关系、风格和所选上下文发送给配置的模型平台。默认直接访问 https://open.bigmodel.cn/api/paas/v4 ，判断、回复、排序共用一个智谱 API Key。平台按自己的条款处理数据；请仅处理你有权使用的内容。设置页的完整流程测试使用内置虚构对话，仍会产生真实 API 请求。

智谱密钥保存在当前用户注册表 HKCU\Environment 的 CHATHELP_ZHIPU_API_KEY，保存后同步到当前进程环境。不是加密保险库，同一用户下的程序可能读取，之后启动的子进程可能继承。旧供应商继续使用各自的 JEV_API_KEY/LLM_API_KEY，智谱不借用它们。卸载免安装目录不会自动删除注册表密钥；可在系统“环境变量”中删除该变量并重启程序。不要在截图、Issue 或日志中分享密钥。

非密钥设置保存在 exe 同目录的 config.json（源码运行时为仓库根目录），包含关系、风格等自填内容。删除该文件可清除设置，转发软件时不要附带自己的 config.json。聊天历史仅保存在运行内存，不自动写入配置文件。

点击“复制”或“填入”会写系统剪贴板，其他软件可能读取；“填入”还会激活微信并模拟粘贴，不按回车、不点发送。可关闭采集或退出程序停止处理。

免费仅指官方当前指定模型的推理价格，不代表永久免费、无限并发或任何其他模型也免费。
