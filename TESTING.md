# 测试范围

本地 Windows 11 x64 / Python 3.11.6 验证：20 项自动化测试通过。

- 智谱三阶段请求、同一凭据、JSON/枚举/概率检查，错误输出不包含响应正文或密钥。
- HTTP 本地服务验证实际请求协议、429 重试、401 终止和禁止重定向；并发请求串行。
- 排序失败不伪造推荐，不在判断失败后盲目生成。
- 新旧配置、独立凭据、保留旧配置和禁用原版更新。
- 窗口设置共用一把智谱密钥；虚构对话测试不自动保存。
- 合成中文图片经过实际 RapidOCR 识别；测试期间阻止 Python socket 连接。
- 过期会话结果不可填入；模拟 Windows 输入调用时仅 Ctrl+End、Ctrl+V，无 Enter 和发送操作。

未验证：真实智谱 Key 的在线质量、实际微信聊天的采集/填入、所有 DPI/多屏/Windows 10 机型。Windows 当前未实现安卓的 QQ 适配。单测使用的输入调用替身不能代替真实微信现场验收。

冻结包另以 `--smoke-test` 检查依赖 DLL、合成中文 OCR、设置窗口和随包许可，结果详见 Release。该检查不采集真实窗口，不接触用户配置；截图只含应用自身的虚构设置界面。
